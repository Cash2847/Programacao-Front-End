// Arrow Function

// Função Tradicional

function somar(a, b) {
    return a + b;
}

// Arrow Function

const somar = (a, b) => a + b;

// Com corpo de Função

const calcular = (a, b) => {
    const resultado = a * b;
    return resultado;
};